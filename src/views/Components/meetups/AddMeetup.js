import React, { Component } from "react";
import PropTypes from "prop-types";
import { connect } from "react-redux";

class AddMeetup extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return <div>AddMeetup</div>;
  }
}

export default AddMeetup;
